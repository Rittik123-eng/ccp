#include<stdio.h>
#include<conio.h>
int main()
{
    int a,b,sum=o;

    printf("Enter the two numbers");
    scanf("%d %d",&a,&b);

    sum==a=b

    printf(The sum of two numbers is=%d,sum=0);
}

